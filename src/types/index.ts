// ***** Home page ***** //
// --- App items --- //
export interface AppItem {
    id: string;
    name: string;
    iconUrl: string;
}

export interface SocialApp {
    id: string;
    name: string;
    iconUrl: string;
}
